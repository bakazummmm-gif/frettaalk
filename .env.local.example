# Supabaseプロジェクトの Settings > API から値をコピーして、
# このファイルを .env.local という名前でコピーして使ってください。
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key

# Settings > API の「service_role」キー。サーバー専用(Webhookや管理操作用)。
# 絶対にNEXT_PUBLIC_を付けない(ブラウザに渡すと全データが誰でも読み書きできてしまう)。
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key

# Stripeダッシュボード(テストモード) > 開発者 > APIキー から取得
NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY=pk_test_xxx
STRIPE_SECRET_KEY=sk_test_xxx

# Stripeダッシュボード > 開発者 > Webhook でエンドポイント作成後に取得
STRIPE_WEBHOOK_SECRET=whsec_xxx
